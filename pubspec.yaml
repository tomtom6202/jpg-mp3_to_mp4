name: my_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^6.1.1
  # 🏆 救星降臨：換成社群最新維護的版本，檔案絕對抓得到！
  ffmpeg_kit_flutter_new: ^4.1.0
  gal: ^2.1.3
  path_provider: ^2.1.1
  screenshot: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
